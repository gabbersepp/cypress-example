{
  "name": "cypress-example",
  "version": "1.0.0",
  "dependencies": {
    "cypress": "^3.6.1",
    "cypress-plugin-retries": "^1.4.0",
    "express": "~4.17.1"
  },
  "devDependencies": {},
  "scripts": {
    "cypress:open": "set CYPRESS_RETRIES=3 && cypress open",
    "cypress:run": "set CYPRESS_RETRIES=3 && cypress run",
    "server": "node server/index.js"
  }
}
